@using CrmApp.Models
@model IEnumerable<CrmApp.Models.Customer>

@{
    ViewBag.Title = "Index";
}
<div class="row">
    <div class="col-sm-4" style="width:25%;margin-top:2%">
        <table>
            <tr>
                <td>
                    @*@Html.ActionLink("All Cities", "Index")*@
                    @Html.ActionLink("All Cities", "Index", null, new { @class = "btn btn-info" })
                </td></tr>
                @using (Html.BeginForm())
                {
                    foreach (var item in ViewData["cities"] as IList<Cities>)
                    {
                        <tr>
                            <td>
                                @Html.ActionLink(item.City, "GetCityCustomers", new { City = item.City }, new { @class = "btn btn-info" })
                            </td>
                        </tr>
                    }
               
                }
             </table>
            </div>

    <div class="col-lg-8" style="width:75%">

        <h2>Index</h2>

        <p>
            <a asp-action="ModalAction" asp-controller="Home" asp-route-id="1" data-target="#exampleModal" data-toggle="modal">Open Modal</a>
            @Html.ActionLink("Add New", "Create",null, new { @class = "btn btn-success"})
        </p>
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Cities.City)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.FirstName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.LastName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Age)
                </th>
                <th>
                    @Html.DisplayText("Edit")
                </th>
                <th>
                    @Html.DisplayText("Delete")
                </th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Cities.City)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FirstName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.LastName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Age)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class = "btn btn-warning", @style = "color:white" })
                    </td>
                    <td>
                        @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class = "btn btn-danger", @style = "color:white" })
                    </td>
                </tr>
            }

        </table>
        </div>
    </div>
@*<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Modal title</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                @Html.Partial("CustomCreate.cshtml")
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary">Save changes</button>
            </div>
        </div>
    </div>
</div>*@

